name: taweda
description: Application Spécifique de Gestion Financière
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0

  provider: ^6.1.1
  pdf: ^3.10.7
  printing: ^5.11.1
  excel: ^3.0.0
  path_provider: ^2.1.2
  intl: ^0.20.2
  image_picker: ^1.0.7
  fl_chart: ^1.2.0
  http: ^1.6.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
